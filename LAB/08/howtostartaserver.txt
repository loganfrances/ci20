open a terminal window
cd into project directory
 type -- php -S localhost:8000 

now type into the browser localhost:8000
this relates to your folder; if you need to open a certain file, append the filename
